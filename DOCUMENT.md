# FDocument

FDocument is a small F# library that provides simple, functional wrappers for working with .NET XML types such as `XDocument` and `XElement`.

It makes common XML operations easier to use in F# by exposing helpers for parsing, loading, saving, navigating, and representing optional values with `Option` or operation errors with `Result`.